package com.phi;

public class EquationFirst {
    public int equationFirst(int a, int b) {
        //int x = 0;
        //ax + b = 0 -> x = -b / a
        //x = -b / a;
        //return x;
        return -b / a;
    }
}
